/// The package's own version, reported to RoamSwitchMCPServer in the
/// JSON-RPC `initialize` handshake as `clientInfo.version`. Keep this in
/// step with the git tag cut for a release.
enum RoamSwitchKitVersion {
    static let current = "1.1.0"
}
